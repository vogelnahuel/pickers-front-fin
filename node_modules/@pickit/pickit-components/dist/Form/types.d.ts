import { ReactNode } from 'react';
import { FormRenderProps, AnyObject } from 'react-final-form';
import { AxiosResponse } from 'axios';
export interface FormProps {
    render?: (props: FormRenderProps<AnyObject>) => ReactNode;
    onSubmit: (values?: any) => Promise<AxiosResponse<any> | any>;
    onSuccess?: (response: AxiosResponse | any) => void;
    onFailure?: (error: Error) => void;
    mutators?: any;
    initValues?: any;
    isAnimated?: boolean;
    resetOnSubmit?: boolean;
    testId?: string;
}
export interface OwnFormRenderProps extends FormRenderProps {
    isSubmitting: boolean;
    isSuccess: boolean;
    isFailure: boolean;
}
