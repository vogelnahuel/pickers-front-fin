import { SliderProps } from '../Slider/types';
export interface ImageGridProps extends SliderProps {
    readonly?: boolean;
    placeholder?: string;
    onChange?: (value: string) => void;
    setError?: (value: boolean) => void;
}
