import { Dispatch, SetStateAction } from 'react';
export interface SliderProps {
    slides: string[];
    index: [number, Dispatch<SetStateAction<number>>];
    value: [string[], Dispatch<SetStateAction<string[]>>];
    readOnly?: boolean;
    deleteImage?: (index: number) => void;
}
