import { FieldRenderProps } from 'react-final-form';
export interface DatePickerProps extends FieldRenderProps<any, HTMLInputElement> {
    placeholder: string;
    labeled?: boolean;
    minDate?: string;
    maxDate?: string;
    outputFormat?: string;
    t?: any;
    testId?: string;
    language: string;
    singleSelection?: boolean;
    readOnly?: boolean;
    disabled?: boolean;
}
