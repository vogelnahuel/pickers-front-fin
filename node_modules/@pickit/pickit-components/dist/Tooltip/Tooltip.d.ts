/// <reference types="react" />
import { ToolTipProps } from './types';
export declare enum ToolTipPosition {
    right = "right",
    bottom = "bottom",
    left = "left",
    top = "top"
}
declare const Tooltip: ({ children, message, disabled, position, }: ToolTipProps) => JSX.Element;
export default Tooltip;
