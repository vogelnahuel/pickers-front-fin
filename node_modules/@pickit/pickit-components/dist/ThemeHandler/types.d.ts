import { Themes } from './ThemeHandler';
export interface ThemeHandlerProps {
    /**
     * Name of theme active theme of the Application, can get the available themes from ``Themes`` enum.
     */
    theme: Themes;
    /**
     * Determinate if the component should load the class name of the theme in the HTML tag.
     */
    useHtmlClassName?: boolean;
}
