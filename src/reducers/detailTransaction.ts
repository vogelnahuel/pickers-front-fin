import {  } from "sagas/types/transactions";
import {  } from "store";
import { } from "./types/transaction";
export const TRANSACTIONS = "DETAIL_TRANSACTIONS";

export const types:any = {
    DETAIL_TRANSACTIONS_REQUEST: `${TRANSACTIONS}_REQUEST`,
    DETAIL_TRANSACTIONS_SUCCESS: `${TRANSACTIONS}_SUCCESS`,
    DETAIL_TRANSACTIONS_ERROR: `${TRANSACTIONS}_ERROR`,
};

export const INITIAL_STATE:any = {

};

export const actions:any = {
  
};

export const selectors:any = {

};


const reducer =(state:any = INITIAL_STATE, action:any ) => {
    
};

export default reducer;